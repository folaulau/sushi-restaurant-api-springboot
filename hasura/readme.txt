Metadata
1. Permission
a. Use console to configure permissions.
b. Run the follow command to export to metadata folder
hasura metadata export --admin-secret test


To apply existing meta data to a new endpoint or new hasura server:
- run run-local-hasura-config.sh